<section>
    <h2>Dashboard</h2>

    <nav>
        <a class="btn" href="<?=buildUrl('admin_article_new');?>"><i class="fas fa-plus-square"></i> Ajouter un article</a>
    </nav>

    <table> 
        <thead>
            <th>#</th>
            <th>Date de création</th>
            <th>Auteur</th>
            <th>Titre</th>
            <th>Actions</th>
        </thead>
        <tbody>
            <?php foreach ($articles as $article): ?>
                <tr id="article-<?=$article['article_id']?>">
                    <td><?=intval($article['article_id']);?></td>
                    <td><?=(new DateTimeImmutable($article['created_at']))->format('d/m/Y');?></td>
                    <td><?=htmlspecialchars($article['firstname'])?> <?=htmlspecialchars($article['lastname'])?></td>
                    <td><?=htmlspecialchars($article['title']);?></td>
                    <td>
                        <a href="<?=buildUrl('admin_article_edit', ['article_id' => $article['article_id']]);?>" title="Modifier"><i class="far fa-edit"></i></a>
                        <a class="delete-button" href="<?=buildUrl('admin_article_delete', ['article_id' => $article['article_id']]);?>" title="Supprimer"><i class="far fa-trash-alt"></i></a>
                    </td>
                </tr>
            <?php endforeach; ?>
        </tbody>
    </table>
</section>

<script src="<?=asset('js/admin.js');?>"></script>