<svg viewBox="0 0 {{ avatar.size }} {{ avatar.size }}" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink">
    {% for rowIndex, row in avatar.grid %}
        {% for colIndex, color in row %}
            <rect x="{{ colIndex }}" y="{{ rowIndex }}" width="1" height="1" fill="{{ color }}" />';
        {% endfor %}
    {% endfor %}
</svg> 